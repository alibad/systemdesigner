{
  "challengeId": "ai-code-assistant",
  "kind": "tradeoff",
  "title": "Model choice for inline code completion",
  "prompt": "Your IDE plugin shows inline \"ghost text\" completions as the developer types. Each keystroke can trigger a new suggestion. Which serving strategy should power the completions?",
  "passThreshold": 0.6,
  "xpWeight": 12,
  "constraint": "Completions must feel instant — under ~300ms end-to-end — or developers turn the feature off entirely. Perceived latency dominates raw suggestion quality.",
  "options": [
    { "id": "large-high-quality", "accepted": false, "weight": 0, "why": "A large model's per-token latency plus network round-trip blows past the 300ms budget on every keystroke. The suggestions are excellent but arrive after the developer has already typed past them, so they disable the feature — exactly the failure the constraint warns about." },
    { "id": "small-fast-model", "accepted": true, "weight": 1, "why": "The constraint says perceived speed beats raw quality, and a small distilled model returns within the 300ms budget so completions appear before the next keystroke. Slightly weaker suggestions are an acceptable trade because the feature only delivers value if it feels instant." },
    { "id": "speculative-cached", "accepted": true, "weight": 0.6, "why": "Caching common prefixes and speculatively prefetching keeps most responses well under 300ms and is defensible. But it adds invalidation and prefetch-accuracy complexity; the simpler small-fast-model already satisfies the latency constraint, so this is a reasonable-but-heavier optimization rather than the cleanest fit." },
    { "id": "large-with-streaming", "accepted": false, "weight": 0, "why": "Streaming helps chat UIs, but inline ghost text needs a complete, coherent suggestion to render — a half-streamed line is useless and the full completion still misses 300ms. It trades the constraint away for quality the developer never gets to see." }
  ]
}
