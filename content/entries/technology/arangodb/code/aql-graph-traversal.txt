FOR vertex, edge, path IN 2..2 OUTBOUND "users/alice"
  GRAPH "social_network"
  PRUNE edge.blocked == true
  FILTER path.edges[*].blocked ALL != true
  RETURN DISTINCT {
    user: KEEP(vertex, "_key", "name"),
    relationship_path: path.edges[*].type
  }
