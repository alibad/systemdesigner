FOR user IN users
  FILTER user.city == @city
  FILTER user.age >= @minimumAge AND user.age <= @maximumAge
  SORT user.created_at DESC
  LIMIT @resultLimit
  RETURN KEEP(user, "_key", "name", "email", "age", "created_at")
