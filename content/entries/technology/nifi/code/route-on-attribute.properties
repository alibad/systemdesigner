# RouteOnAttribute dynamic properties.
# Each key becomes a relationship and each value must return a boolean.
priority-high=${priority:equalsIgnoreCase('high')}
small-payload=${fileSize:lt(1048576)}
within-retry-budget=${retry.count:toNumber():lt(5)}
