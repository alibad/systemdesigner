---
registryId: tools-rate-limit-tester
---

{% tool-context
   title="Rate Limiter Workbench"
   question="How do you protect a backend without making healthy callers share an abusive caller's fate?"
   definition="A rate limiter enforces a time-bounded admission contract for a specific identity. Its algorithm controls when budget is spent, its identity key controls fairness, and its failure policy decides whether limiter availability or backend safety wins."
   inputs="Choose the algorithm, request budget, time window, burst or queue capacity, identity key, traffic mix, retry behavior, regions, counter lag, backend capacity, and fail-open or fail-closed behavior."
   outcome="Trace tokens or windows, admitted and rejected requests, queueing, caller fairness, retry amplification, distributed overshoot, response headers, backend pressure, and the user-visible consequence."
   experiment="Compare a launch burst, abusive tenant, stale distributed counter, retry storm, counter-store outage, and controlled recovery. Keep the backend inside its tested capacity without throttling healthy tenants unnecessarily." /%}

{% interactive-block id="tools/rate-limiter" /%}

{% quiz
   questionsFile="/api/content/tools/rate-limiter/quiz/rate-limiter-review.json"
   lessonSlug="tools-rate-limit-tester"
   title="Rate Limiter Lab Review" /%}
